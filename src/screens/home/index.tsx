import React from 'react';
import { View } from 'react-native';

// Define the functional component with TypeScript
const Home: React.FC = () => {
  return (
    <View />
  );
};

export default Home;